# Status icon

Drop a `status.png` here (square, ideally 512×512, transparent background) to use
your own Nova icon. It gets tinted per state — green / yellow / red.

If `status.png` is absent at build time, Nova generates a plain colored dot
instead. No asset is required for Nova to run.
